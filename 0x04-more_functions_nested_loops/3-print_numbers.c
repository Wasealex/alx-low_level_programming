#include "main.h"
/**
 *print_numbers - prints numbers from 0 to 9
 *
 *
 *Return: numbers if no error
 */
void print_numbers(void)
{
	int a;

	for (a = 48; a <= 57; a++)
		_putchar(a);
	_putchar('\n');
}
